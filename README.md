# Typescript + Next + Express Boilerplate

### Commands

First run `npm i` in root 

- `npm run install` installs all dependencies
- `npm run dev` starts dev mode
- `npm run build` builds the project
- `npm run prod` starts the built project